# Default Build Configuration (MousePaw Media Build System)
# Version: 3.3.0

######################
# USAGE INSTRUCTIONS
# This file contains the default search paths for all libraries your different
# CMakeLists.txt files need to find. Set this up and place it in the root of
# your repository.
# (You may ignore the DO NOT MODIFY warning; that's for your users.)
#
# Be sure to leave 'set(LLVM false)' in place!!!
#
# Remove these instructions after putting the file in place.
######################

# DO NOT MODIFY THIS FILE!
# Use build.config.txt to change the path.

set(LLVM false)

set(EIGEN_DIR
	${CMAKE_HOME_DIRECTORY}/../../libdeps/libs
)

set(EVENTPP_DIR
	${CMAKE_HOME_DIRECTORY}/../../libdeps/libs
)

set(OPUS_DIR
	${CMAKE_HOME_DIRECTORY}/../../libdeps/libs
)

set(PUGIXML_DIR
	${CMAKE_HOME_DIRECTORY}/../../libdeps/libs
)

set(ARCTICTERN_DIR
	${CMAKE_HOME_DIRECTORY}/../../arctic-tern/arctic-tern
)

set(GOLDILOCKS_DIR
	${CMAKE_HOME_DIRECTORY}/../../goldilocks/goldilocks
)

set(IOSQUEAK_DIR
	${CMAKE_HOME_DIRECTORY}/../../iosqueak/iosqueak
)

set(ONESTRING_DIR
	${CMAKE_HOME_DIRECTORY}/../../onestring/onestring
)

set(NIMBLY_DIR
	${CMAKE_HOME_DIRECTORY}/../../nimbly/nimbly
)

set(SIMPLEXPRESS_DIR
	${CMAKE_HOME_DIRECTORY}/../../simplexpress/simplexpress
)
